/**
 * Created by kyle on 8/6/16.
 */

module.exports = {
    caculateHotScore: function (image) {
        var logValue = image.view_count === 0 ? 1 : image.view_count;
        return Math.log(logValue) + image.created_at.getTime() / 30000000.0;
    }
}