/**
 * Created by kyle on 8/6/16.
 */

module.exports = {
    caculateHotScore: function (image) {
        return image.view_count + image.created_at.getTime() / 45000.0;
        logger.debug('created time int = ' + image.created_at.getTime());
    }
}